{
  "name": "memnest-learn",
  "version": "0.1.0",
  "description": "Client-side learning + working-memory + KV-cache-stable injection layer on top of the memnest engine. Keeps memnest's Rust core LLM-free; borrows the host agent's model for extraction/consolidation.",
  "type": "module",
  "main": "dist/index.js",
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node --format esm --packages external",
    "prepublishOnly": "bun run build",
    "prepare": "bun run build",
    "test": "bun test test/",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["pi", "memory", "memnest", "mcp", "rag"],
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.78.0",
    "@earendil-works/pi-ai": "^0.78.0",
    "@sinclair/typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "bun-types": "^1.3.14",
    "typescript": "^5.6.0"
  }
}
